{% extends 'base.html.twig' %}

{% if app.request.attributes.get('_route') == 'app_admin_devis_edit' %}
    {% set titre = 'Editer le devis' %}
{% else %}
    {% set titre = 'Créer un devis' %}
{% endif %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
    {{ component('breadcrumbs', {'title': block('title')}) }}

    <section class="auth">
        <div class="container">
            <div class="row justify-content-center">
                <div class="info-box mb-4">
                    <div class="container mt-3">
                        {% for label, messages in app.flashes %}
                            <div class="alert alert-{{ label }}">
                                {% for message in messages %}
                                    {{ message | raw }}    
                                {% endfor %}
                            </div>
                        {% endfor %}
                        {{ component('devis_form_admin', {
                            devi: devi.id ? devi: null,
                            form: form
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
