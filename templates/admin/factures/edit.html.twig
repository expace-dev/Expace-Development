{% extends 'base.html.twig' %}

{% if app.request.attributes.get('_route') == 'app_admin_factures_edit' %}
    {% set titre = 'Editer la facture' %}
{% else %}
    {% set titre = 'Créer une facture' %}
{% endif %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
    {{ component('breadcrumbs', {'title': block('title')}) }}

    <section class="auth">
        <div class="container">
            <div class="row justify-content-center">
                <div class="info-box mb-4">
                    <div class="container mt-3">
                        {{ component('factures_form_admin', {
                            facture: facture.id ? facture: null,
                            form: form
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
