{% extends 'base.html.twig' %}

{% block title %}Factures{% endblock %}

{% block body %}
    <h1>Factures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ facture.statut }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ facture.url }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ facture.createdAt ? facture.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Services</th>
                <td>{{ facture.services ? facture.services|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ facture.amount }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ facture.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_factures_index') }}">back to list</a>

    <a href="{{ path('app_factures_edit', {'id': facture.id}) }}">edit</a>

    {{ include('factures/_delete_form.html.twig') }}
{% endblock %}
