<form method="post" action="{{ path('app_temoignages_delete', {'id': temoignage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ temoignage.id) }}">
    <button class="btn">Delete</button>
</form>
