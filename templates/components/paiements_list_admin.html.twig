<div class="row">
    <div class="col-lg-3">
        <form action="" class="input-icon my-3 my-lg-0">
            <div class="gs-container">
                <input type="text" class="form-control header-search">
            </div>
            <div class="input-icon-addon">
                <i class="fa-solid fa-magnifying-glass"></i>
            </div>
        </form>
    </div>
</div>
<div class="table-responsive mt-2">
    <table class="table card-table table-vcenter table-hover">
        <thead class="thead-dark form-group-sm">
            <tr>
                <th>Facture N°</th>
                <th>Montant</th>
                <th>Date</th>
                <th class="text-end"><select name="search_limit" class="search_limit" style="width: 60px;"><option value="5">5</option><option value="15" selected="selected">15</option><option value="25">25</option><option value="50">50</option><option value="100">100</option><option value="999999999">all</option></select></th>
            </tr>
        </thead>
        <tbody>
            {% for paiement in this.allPaiements.data %}
                <tr>
                    <td>{{ paiement.facture.slug }}</td>
                    <td>{{ paiement.montant }} €</td>
                    <td>{{ paiement.createdAt ? paiement.createdAt|date('j F Y') : '' }}</td>
                    <td class="text-end">
                        <div class="btn-group">
                            <a class="btn btn-secondary" target="_blank" href="{{ path('app_admin_factures_show', {'slug': paiement.facture.slug}) }}"><i class="fas fa-eye"></i></a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <div style="padding-right: 10px;">
        {% set pages = this.allPaiements.pages %}
        {% set currentPage = this.allPaiements.page %}
        {% set limit = this.allPaiements.limit %}
        {% if pages > 1 %}
            <ul class="pagination justify-content-end">
                {% if currentPage > 1 %}
                    <li class="page-item"><a class="page-link" href="{{ path('app_admin_paiements_index', {page: currentPage - 1}) }}">&laquo;</a></li>
                {% else %}
                    <li class="page-item disabled"><a class="page-link">&laquo;</a></li>
                {% endif %}
                {% set pageSupp = currentPage +4 %}
                {% if pages > 5 and currentPage < pages %}
                    {% for page in currentPage..pageSupp %}
                        {% if page <= pages %}
                            <li class="{{ (page == currentPage) ? 'active' : '' }} page-item">
                                <a class="page-link" href="{{ path('app_admin_paiements_index', {page: page, limit: limit }) }}">{{ page }}</a>
                            </li>
                        {% endif %}
                    {% endfor %}
                {% else %}
                    {% for page in 1..pages %}
                        <li class="{{ (page == currentPage) ? 'active' : '' }} page-item">
                            <a class="page-link" href="{{ path('app_admin_paiements_index', {page: page, limit: limit }) }}">{{ page }}</a>
                        </li>
                    {% endfor %}
                {% endif %}
                {% if currentPage < pages %}
                    <li class="page-item"><a class="page-link" href="{{ path('app_admin_paiements_index', {page: currentPage + 1}) }}">&raquo;</a></li>
                {% else %}
                    <li class="page-item disabled"><a class="page-link">&raquo;</a></li>
                {% endif %}
            </ul>
        {% endif %}
    </div>
</div>
