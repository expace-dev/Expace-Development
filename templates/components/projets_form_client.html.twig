<div {{ attributes }} >
    {{ form_start(form, {attr: {'data-model': '*'}}) }}
        <div class="row">
            <div class="col-lg-12">{{ form_row(form.titre) }}</div>
            <div class="col-lg-12">{{ form_row(form.besoinClient) }}</div>
            <div class="col-lg-12 text-end">
                <button class="btn btn-success">Enregistrer</button>
                <a class="btn btn-secondary" href="{{ path('app_client_projets_index') }}">Annuler</a>
            </div>
    {{ form_end(form) }}
</div>